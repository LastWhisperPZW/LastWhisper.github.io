#include "stdafx.h"
#include<iostream>

using namespace std;

template<class Ta>
int num(Ta a[],Ta b[],int n)
{
	int sum=0,i;
	for (i=0;i<n;i++)
	{
		sum += a[i] * b[i];
	}
	return sum;
}


int main()
{
	int a[] = { 1,2,3};
	int b[] = { 4,5,6 };	
	cout << num(a, b, 3) << endl;
}
